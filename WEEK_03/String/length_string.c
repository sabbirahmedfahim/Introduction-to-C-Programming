#include<stdio.h>
int main()
{
    char arr[100] = "hello world";
    int i = 0, count = 0;
    while (arr[i]!='\0')
    {
        arr[i];
        i++;
        count++;
    }
    printf("Length of the array: %d", count);
    
    
    return 0;
}