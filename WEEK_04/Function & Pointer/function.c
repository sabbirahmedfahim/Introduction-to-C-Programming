// types of function: int, float, char, double, void
// they all return values, whereas void does not return a value.

// depends on return type and variable, there are four kinds of function:
// 1. return type + parameter
// 2. return type + no parameter
// 3. no return type + parameter
// 4. no return type + no parameter


#include <stdio.h>
    
// return_type name(parameter) 
// {
//     code
//     return what?
// }
int main()
{
    //call function

    return 0;
}