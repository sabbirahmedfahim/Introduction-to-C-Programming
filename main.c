#include <stdio.h>
#include <string.h>
int main()
{
    char ch;
    char stng[100];
    char sen_stng[100];
    scanf("%c", &ch);
    scanf("%s", &stng);
    printf("%c\n", ch);
    printf("%s\n", stng);

    return 0;
}